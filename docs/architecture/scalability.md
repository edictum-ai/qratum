Forward design reference.
This document does not expand the current implementation scope.
Current executable scope is defined only in SPEC.md.

# Scalability

Qratum scales by capturing at the edge, keeping hooks tiny, processing
asynchronously, syncing redacted bundles later, keeping APIs stateless, using
workers for heavy processing, storing large artifacts in object storage, and
isolating any future marketplace plane.
